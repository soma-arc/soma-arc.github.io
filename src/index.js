import Vue from 'vue';

window.addEventListener('load', () => {
    /* eslint-disable no-new */
    // const app = new Vue({
    //     el: '#app',
    //     data: d,
    //     render: (h) => {
    //         return h('root', { 'props': d })
    //     },
    //     components: { 'root': Root }
    // })
});
